source: 'https://github.com/Sabayon/packer-templates'
directory: '/'
image: 'sabayon/packer-amd64'
binds:
   - /dev/vboxdrv:/dev/vboxdrv
script:
   - packer build -var 'flavor=Server' -var 'vagrant=vagrant' -var 'guest_additions=virtualbox' -var 'extra=extra' -only virtualbox-iso images.json
   - mv *.box artefacts/
queue: 'virtualbox'
task: docker_execute
tag_namespace: "vagrant"
publish_mode: "append"
planned: "0 0 10 * * *"
