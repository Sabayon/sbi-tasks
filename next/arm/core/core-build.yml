repository:
  description: Core Arm repo
build:
  emerge:
    # Install each package separately
    split_install: 1
    preserved_rebuild: 1
    default_args: --accept-properties=-interactive -t --quiet --complete-graph --newuse --deep --with-bdeps=y -j --buildpkg
  target:
    - sys-apps/systemd
